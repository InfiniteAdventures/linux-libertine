\relax

% t1os.etx -- install a T1-encoded roman font with oldstyle digits
%
% We do not need to distinguish between roman and italic in T1,
% hence we simply call t1.etx with oldstyle parameters.

\encoding

\setcommand\digit#1{#1.oldstyle}

\inputetx{t1}

\endencoding
